import React from 'react';
import PropTypes from 'prop-types';

class classe extends React.Component {
  render() {

    return (
      <div>

      </div>
    );
  }
}

classe.propTypes = { rating: PropTypes.number.isRequired };

export default classe;