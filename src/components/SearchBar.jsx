import React from 'react';

class SearchBar extends React.components {
  render() {
    return (
      <form>
        <input type="text" />
        <input type="checkbox" />
        <select></select>
      </form>
    );
  }
}

export default SearchBar;
